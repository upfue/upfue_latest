import React from 'react';
import Wrapper from './Wrapper/BigSideBar';

const BigSideBar = () => {
  return (
    <Wrapper>
      <h4>BigSideBar</h4>
    </Wrapper>
  );
};

export default BigSideBar;
